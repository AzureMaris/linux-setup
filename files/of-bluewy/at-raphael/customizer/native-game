#!/usr/bin/env nu

use ./lib [ steam_env mangohud_config_values ]

def --wrapped main [...rest]: nothing -> nothing {
   with-env {
      MANGOHUD_CONFIG: (
         $mangohud_config_values
         | str join ','
      )
   } {
      exec mangohud game-performance ...$rest
   }
}
