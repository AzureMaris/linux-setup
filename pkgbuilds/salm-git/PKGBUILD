pkgname=salm-git
_pkgname=salm
pkgver=r8.a4526a3
pkgrel=1
pkgdesc='A way to manage dots'
arch=(any)
url="https://github.com/BluewyDiamond/$_pkgname"
depends=(nushell)
provides=($_pkgname)
conflicts=($_pkgname $_pkgname-bin $_pkgname-git)
source=("git+$url.git")
sha256sums=('SKIP')

pkgver() {
   cd "$srcdir/$_pkgname"
   printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short=7 HEAD)"
}

package() {
   cd "$srcdir/$_pkgname"
   install -Dm755 salm -t "$pkgdir/usr/bin"
}
